package org.cld.crbook.util.test;

import java.util.List;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.cld.crbook.util.BookHandler;
import org.cld.datastore.entity.Product;
import org.cld.datastore.impl.HibernateDataStoreManagerImpl;
import org.junit.Test;

public class BookUtilTest {
	private static Logger logger =  LogManager.getLogger(BookUtilTest.class);

}
